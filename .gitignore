node_modules/
dist/

# built by scripts/build-explorer.mjs; published to Pages by CI, never committed
docs/
.env
.env.local
*.log

# the agent-wallet's rolling 24h spend ledger — local state, never shared
.wallet-state.json

# local-only grant working doc
GRANT.md
